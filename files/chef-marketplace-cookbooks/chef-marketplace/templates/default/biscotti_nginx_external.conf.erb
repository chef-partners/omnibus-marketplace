location ~ "^/biscotti/setup" {
  access_by_lua_file '<%= node["chef-marketplace"]["biscotti"]["nginx"]["biscotti_lua_file"] %>';

  proxy_redirect http://biscotti /;
  proxy_pass http://biscotti;
}

location ~ "^/biscotti" {
  proxy_redirect http://biscotti /;
  proxy_pass http://biscotti;
}
